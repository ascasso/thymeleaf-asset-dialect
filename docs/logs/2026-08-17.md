# 2026-08-17

## Gradle wrapper upgrade and change-tracking instructions

- Upgraded the Gradle wrapper from 8.14.3 to 9.7.0.
- Regenerated the wrapper JAR and POSIX/Windows launcher scripts, and updated `gradle-wrapper.properties`.
- Added repository instructions requiring every change to be recorded in a dated `docs/logs/` entry and committed in focused Git commit(s).
- Verification: `./gradlew --version` reported Gradle 9.7.0 on the local Java 26/macOS environment.
- `./gradlew test --no-daemon` could not resolve the Java 21 toolchain required by `core` and `sample`; the local Java 26 installation is available, but toolchain download repositories are not configured.
- `./gradlew help --no-daemon` completed successfully with Gradle 9.7.0.

## Spring Boot and Java upgrade

- Upgraded Spring Boot from 3.5.3 to 4.1.0, the latest stable release at the time of this change.
- Updated the project and sample toolchains from Java 21 to Java 25.
- Replaced the deprecated `spring-boot-starter-web` with `spring-boot-starter-webmvc` and aligned Thymeleaf with Spring Boot 4.1.0's managed 3.1.5 release.
- Verification: `JAVA_HOME=/Library/Java/JavaVirtualMachines/graalvm-25.jdk/Contents/Home ./gradlew test --no-daemon --rerun-tasks` passed; the root, `core`, and `sample` test tasks completed successfully under Java 25.

## `tad:href` support and repository guidance

- Added `tad:href` processing alongside the existing `tad:src` support while preserving the original public `AssetProcessor` constructor.
- Added unit and sample-template regression coverage for standard `href` and existing `src` output.
- Corrected public examples to use the default `tad` prefix and documented stylesheet usage.
- Consolidated project guidance in `AGENTS.md`, updated the remaining guidance reference, and removed the obsolete `CLAUDE.md` file.
- Verification: `JAVA_HOME=/Library/Java/JavaVirtualMachines/graalvm-25.jdk/Contents/Home ./gradlew :core:test :sample:test --no-daemon --rerun-tasks` and `JAVA_HOME=/Library/Java/JavaVirtualMachines/graalvm-25.jdk/Contents/Home ./gradlew clean build --no-daemon --rerun-tasks` passed. A local `GET /` smoke test returned a versioned standard stylesheet `href` and no rendered `tad:href` attribute.
